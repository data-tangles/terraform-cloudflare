name: Terraform Validation CI

on:
  - pull_request

permissions:
  contents: write

jobs:
  validation:
    name: Run Validation Checks 
    runs-on: ubuntu-latest

    steps:

    - name: Generate GitHub App Token 🪙
      uses: tibdex/github-app-token@v2
      id: generate-token
      with:
        app_id: ${{ secrets.TANGLES_BOT_APP_ID }}
        private_key: ${{ secrets.TANGLES_BOT_PRIVATE_KEY }}

    - name: Checkout 🔔
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.pull_request.head.ref }}
        token: ${{ steps.generate-token.outputs.token }}

    - name: Generate Terraform Docs 🗒️
      id: push-terraform-docs-changes
      uses: terraform-docs/gh-actions@v1.1.0
      with:
        find-dir: .
        output-file: README.md
        output-method: replace
        git-push-user-name: "tangles-bot[bot]"
        git-push-user-email: "169585682+tangles-bot[bot]@users.noreply.github.com"
        git-push: "true"
        git-commit-message: "docs: 🤖 Update Terraform Docs for Workflow Run ID: ${{ github.run_id }} in Branch: ${{ github.head_ref }}"

  changelog:
    name: Generate Changelog 🗒️
    runs-on: ubuntu-latest
    needs:
      - validation
      
    steps:

    - name: Generate GitHub App Token 🪙
      uses: tibdex/github-app-token@v2
      id: generate-token
      with:
        app_id: ${{ secrets.TANGLES_BOT_APP_ID }}
        private_key: ${{ secrets.TANGLES_BOT_PRIVATE_KEY }}

    - name: Checkout 🔔
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.pull_request.head.ref }}
        token: ${{ steps.generate-token.outputs.token }}

    - name: Conventional Changelog Action 📋
      id: changelog
      uses: TriPSs/conventional-changelog-action@v5
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        git-user-name: "tangles-bot[bot]"
        git-user-email: "169585682+tangles-bot[bot]@users.noreply.github.com"
        git-branch: "${{ github.head_ref }}"
        skip-on-empty: "true"
        skip-ci: "false"
        release-count: 10
        version-file: package.yaml
        create-summary: true      
